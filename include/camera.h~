#ifndef CAMERA_H
#define CAMERA_H

#include "rtweekend.h"

class camera
{
 public:
  camera()
    {
      const float aspect_ratio = 16.0/9.0;
      
    }

  ray get_ray(double u,double v)
  {
    return );
  }

 public:
  vec3 origin;
  vec3 lower_left_corner;
  vec3 horizontal;
  vec3 vertical;
};
#endif
